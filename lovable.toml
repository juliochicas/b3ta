[run]
dev = "python3 scripts/static_server.py"
build = "bash -c 'mkdir -p dist && rsync -a --delete --exclude dist --exclude node_modules --exclude .git --exclude supabase --exclude scripts ./ dist/'"
"build:dev" = "bash -c 'mkdir -p dist && rsync -a --delete --exclude dist --exclude node_modules --exclude .git --exclude supabase --exclude scripts ./ dist/'"
